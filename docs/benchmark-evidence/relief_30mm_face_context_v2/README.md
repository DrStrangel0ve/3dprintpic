# 30 mm face and context-selection evidence

This directory contains aggregate telemetry for the private exact-input replay described in `docs/relief-face-context-v2.md`.

Personal source images, masks, depth arrays, renders, HTTP response payloads, and STL files are intentionally excluded. `summary.json` contains the measurements needed to replay the promotion decision without publishing those assets.
